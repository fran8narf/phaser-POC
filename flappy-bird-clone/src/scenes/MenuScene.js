import Phaser from "phaser";

class MenuScene extends Phaser.Scene {
    constructor(config) {
        super('MenuScene');
    }

    preload() {}
    create() {}
    update() {}
}

export default MenuScene;